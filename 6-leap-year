public Boolean isLeapYear(Integer year) {
    if(Math.mod(year,4) == 0) return TRUE;
    else if(Math.mod(year,4) == 0 and Math.mod(year,100) == 0) return FALSE;
    else return false;  //code here
}
