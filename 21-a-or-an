public String aOrAn(String word) {
       if(word.startswith('a') || word.startswith('e') || word.startswith('i') || word.startswith('o') || word.startswith('u')) return 'an '+word; else return 'a '+word;}